import java.util.ArrayList;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.Collections;
abstract class GenericList<T> implements Iterabler<T>{
    //Interator<T> iterator();
    //head (this is the head of the list and should be private).
    //private  Node<T>;
   // private int length; // (the length of the list and should be private)
}
